apiVersion: v1
kind: Service
metadata:
  name: {{ include "tinyurl-chart.fullname" . }}
  labels:
    {{- include "tinyurl-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tinyurl-chart.selectorLabels" . | nindent 4 }}
